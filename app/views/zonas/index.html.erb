<p id="notice"><%= notice %></p>

<h1>Listing Zonas</h1>

<table>
  <thead>
    <tr>
      <th>Cidade</th>
      <th>Estado</th>
      <th>Representante</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @zonas.each do |zona| %>
      <tr>
        <td><%= zona.cidade %></td>
        <td><%= zona.estado %></td>
        <td><%= zona.representante_id %></td>
        <td><%= link_to 'Show', zona %></td>
        <td><%= link_to 'Edit', edit_zona_path(zona) %></td>
        <td><%= link_to 'Destroy', zona, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Zona', new_zona_path %>
